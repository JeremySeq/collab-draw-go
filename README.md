# Collaborative Drawing Web App in Golang

A real-time collaborative drawing app using Websockets. Users can draw together, see each other's cursors, and chat in real time. Built with Golang for the backend and plain HTML, CSS, Javascript for the frontend.

Run with `go run main.go` in `/backend`.
